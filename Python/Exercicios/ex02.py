#Calcular e exibir a área de um quadrado, a partir do valor de sua aresta que será digitado.

a = int(input('Digite valor do lado do quadrado: '))
area = a*a 
print(f'A area do quadrado e: {area}')
